pipeline {
    agent any {
        stages {
            stage('terraform initialization') {
                steps{
                    sh "terraform init"
                }

            }
        }
    }
}